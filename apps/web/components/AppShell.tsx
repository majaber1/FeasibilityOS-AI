"use client";

/**
 * Re-export for clarity — AppChrome is the authenticated/marketing shell switcher.
 * Prefer importing AppChrome from @/components/AppChrome in new code.
 */
export { AppChrome as AppShell } from "./AppChrome";
